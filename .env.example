# compose.yaml 使用的可选覆盖值。
# 只有默认配置不合适时，才复制为不会提交到 Git 的 .env 文件。
POSTGRES_DB=finops
POSTGRES_USER=finops
# 仅供本地开发使用；生产环境密码必须存放在密钥管理服务中。
POSTGRES_PASSWORD=finops_dev_password
# 宿主机暴露端口；容器内部的 PostgreSQL 始终监听 5432。
POSTGRES_PORT=5432
